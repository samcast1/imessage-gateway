services:
  gateway:
    build: .
    container_name: imessage-gateway
    restart: unless-stopped
    environment:
      - WHOOP_API=http://whoop-api:8000
    ports:
      - "8000:8000"
    networks:
      - automation

networks:
  automation:
    external: true